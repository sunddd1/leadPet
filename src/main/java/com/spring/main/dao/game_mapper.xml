<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>

<!-- 누가 이 XML을 사용할까? 누구와 연결? -->
<mapper namespace="com.spring.main.dao.GameDAO">
	 
	<select id="quizList"  resultType="com.spring.main.dto.GameDTO">
		SELECT * FROM quiz ORDER BY quiz_idx
	</select>
	<select id="nemoList"  resultType="com.spring.main.dto.GameDTO">
		SELECT * FROM nemo ORDER BY nemo_idx
	</select>

</mapper>